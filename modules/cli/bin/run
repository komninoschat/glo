#!/usr/bin/env node

const fs = require('fs');
const path = require('path');
const project = path.join(__dirname, '../tsconfig.json');
const dev = fs.existsSync(project);

// if (dev) {
//   require('ts-node').register({ project });
// }

// console.log('dev', dev);

require(`../lib/glo.js`)
  .run()
  // require(`../${dev ? 'src/glo.ts' : 'lib/glo.js'}`)
  //   .run()
  .catch(require('@oclif/errors/handle'));
