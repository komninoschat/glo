import 'mocha';
import { Interpeter } from '../lib/PseudoglossaInterpreter';

describe('@glossa-glo/pseudoglossa-interpreter', () => {
  it('should work', () => {
    console.log(Interpeter);
  });
});
